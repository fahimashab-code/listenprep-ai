// The local prototype uses short simulated Part audio. Development-only
// advance controls are exposed separately through `?demo=true`.
export const SIMULATED_PART_SECONDS = 90;

export const PRODUCTION_PART_PREVIEW_SECONDS = 30;
export const DEMO_PART_PREVIEW_SECONDS = 5;

export const PRODUCTION_FINAL_REVIEW_SECONDS = 120;
export const DEMO_FINAL_REVIEW_SECONDS = 30;
