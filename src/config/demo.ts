// Accelerates part transitions and exposes a clearly labelled advance control
// so the frontend demo can be tested without waiting for a 30-minute recording.
export const DEMO_TIMING = true;
export const DEMO_PART_SECONDS = 90;
