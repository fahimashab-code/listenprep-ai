<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## Authentication

Before changing authentication code, read:

`docs/AUTH_SYSTEM_BLUEPRINT.md`

Implement authentication inside this existing application. Do not create a separate app or replace the existing UI architecture.

Basic Cognito authentication must work without Lambda or API Gateway. Any Lambda/API integrations must remain optional and must not prevent the application from starting when their environment variables are missing.


Read the authentication blueprint first.
Preserve the existing IELTS functionality.
Use Next.js 16 conventions.
Do not expose secrets.

IF you need any information ask me, dont assume anything that will cost
For ui no need to implement fency UI.
Dont complicate thgins.
Dont use playwrite, or other testing automation app, it will take time and cost token proabably

when you stuck on a long time on a task due to some failure..ask me.

